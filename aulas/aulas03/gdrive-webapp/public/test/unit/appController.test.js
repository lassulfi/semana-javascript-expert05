import { describe, test, expect, jest } from "@jest/globals";

describe("#AppController", () => {
    describe("#initialize", () => {
        test.todo("Should initialize the frontend application");
    });

    describe("#onProgress", () => {
        test.todo("Should update the upload progress");
        test.todo("Should stop the update progress if is 98 percent");
    });

    describe("#updateProgress", () => {
        test.todo("should update the total progress in the view manager");
    });

    describe("#onFileChange", () => {
        test.todo("Should upload a file and update the status");
    });

    describe("#updateCurrentFiles", () => {
        test.todo("Should update the main view with a list of all files uploaded");
    })
})