{
  "name": "@erickwendel/gdrive-webapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npx http-server public",
    "dev": "npx http-server public --cert certificates/cert.pem --key certificates/key.pem --ssl -o",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules npx jest --watch --runInBand",
    "test:cov": "NODE_OPTIONS=--experimental-vm-modules npx jest --no-cache --runInBand --coverage"
  },
  "keywords": [],
  "author": "erickwendel",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.12.3"
  },
  "engines": {
    "node": "16"
  },
  "devDependencies": {
    "jest": "^25.5.4"
  }
}
